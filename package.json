{
	"title": "Twinkle",
	"name": "twinkle",
	"private": true,
	"repository": "Xi-Plus/twinkle",
	"scripts": {
		"lint": "eslint .",
		"lint:fix": "eslint . --fix",
		"patchtest": "node scripts/patch-test.js",
		"server": "node scripts/server.js",
		"sync": "perl scripts/sync.pl",
		"test": "jest",
		"deployall": "perl scripts/sync.pl --mode=deploy --all"
	},
	"devDependencies": {
		"eslint": "^7.32.0",
		"eslint-plugin-es5": "^1.5.0",
		"jest": "^27.0.6",
		"mock-mediawiki": "^1.1.0"
	}
}
